model-name: gpt-4o-mini-2024-07-18
max-tokens: 16
temperature: 0.0

system-prompt-template: >-
    As a language expert, identify the main language of the given text and provide its ISO 639 code.

    The output must be a JSON-formatted dictionary with the following key:
    - "language" (string)

    The `language` key should contain the identified language.

    Only provide the JSON dictionary without any additional explanation or text.

human-prompt-template: |
    Text:

    {text}
